interface:
  display_name: "Filet"
  short_description: "Configure Filet rules, JavaScript, and macOS daemon"
  default_prompt: "Use $filet to install Filet, configure my file rules, and verify their plans before enabling the daemon."
